# 2026-09-07

## DesignerApp.vue 拆分解耦（方案讨论，未动码）
- 现状：`src/components/designer/DesignerApp.vue` 2813 行 = script 1329 + template 895 + style 585，一个文件承担 8 个职责（文档/历史持久化、视图模式、选择系统、节点 CRUD 与拖拽落点提交、Inspector 30+ updater、填充数据 IO、895 行模板、585 行 CSS）。
- 建议四批次（每批独立全量 vitest + vue-tsc 绿灯）：
  1. composable 抽取（零 DOM 变更、测试零改动）：`useSchemaDocument`(schema/commit/undo/redo/dirty/localStorage/导入导出) / `useNodeSelection`(选中+层级循环+结构树+isInsideTable) / `useSchemaEdits`(30+ updateXxx，供 Inspector 以 API 对象注入) / `useFillData`(导入导出保存读取)。
  2. `InspectorPanel.vue` + `inspectors/*` 八类型子组件（模板 -640 行）：page/grid/grid-cell/text/p/image/html/table。
  3. `DesignerToolbar.vue` / 左栏（Palette+结构树）/ `IssuesPanel.vue`。
  4. CSS 随组件拆分，公共控件类（`.v2-control`/`.v2-toolbar__button` 等）提到 `designer/styles/designer-ui.css`（非 scoped，避免 scoped 跨组件失效）。
- 风险点（动手前必须核对）：测试依赖的 data 属性必须原样保留 —— `data-palette` / `data-view-mode` / `data-dimension` / `data-grid` / `data-cell-default` / `data-cell-merge` / `data-cell-split` / `data-cell-padding` / `data-field-default` / `data-field-width` / `data-field-inner-border` / `data-paginate`；Inspector 更新函数被 `DesignerApp.test.ts` / `DesignerApp.fillData.test.ts` / `DesignerApp.pagination.test.ts` 经 DOM 事件驱动。
- 判断：P9.1c 专用控件落地必然再动 Inspector 与 updater 集合，**先拆 1+2 再补控件**比继续往 2813 行里堆更划算。

## 批次 1 已落地（用户选定，三十二续）
- 新增 `src/components/designer/composables/`：`useSchemaDocument.ts` / `useNodeSelection.ts` / `useSchemaEdits.ts` / `useFillData.ts`。
- `DesignerApp.vue` 2813 → 1760 行（script 1329 → 279），只剩「装配四个 composable + 视图模式/分页/快捷键/校验派生 + 895 行模板 + 585 行 CSS」。模板与所有 `data-*` 选择器零改动 → 三个 DesignerApp 测试零改动全绿。
- 关键接线（避免后人在拆分中踩坑）：
  - `useSchemaDocument(initialSchema?, { onReset })`：`onReset` 只在「读取本地 / 导入文件」成功替换文档后触发，宿主在此清选中；`resetHistory` 本身不触发（调用方 loadSample/resetBlank/import 已自行清）。
  - 声明顺序坑：`onReset` 需要 `clearSelection`，而 `clearSelection` 来自 selection（selection 又依赖 document 的 schema）→ 用 `let onDocumentReset: () => void = () => {}` 占位、装配后赋值，避免 TDZ。
  - undo/redo 在**壳层**包装（`schemaDoc.undo(); clearSelection();`），composable 保持纯粹。
  - `useSchemaEdits({ document, selection, editable, clearSelection })`：editable 传 getter（不在创建时求值），一切结构编辑经单一闸门。
  - `useFillData({ canvasEl, isPreview, enterPreview })`：导入/读取本地后经 `enterPreview(data)` 进入预览态（避免宿主用 seed 覆盖刚导入的数据）。
- 新增测试 `composables/__tests__/useSchemaDocument.test.ts`(7) + `useSchemaEdits.test.ts`(4)：首次让 commit/undo/redo/tag 合并窗口/持久化/编辑闸门脱离 DOM 单测。
- 验证：vue-tsc 干净；全量 vitest **252/252（31 文件）**。
- 注意：`git status` 中 `src/components/renderer-v2/GridSchemaNode.vue` 的改动是此前遗留、非本次所为，勿误提交/回滚。

## 弹性布局 Flex（三十五续，按用户澄清改为 cell 级）
- 用户需求：弹性格子 Flex，功能定位与 Grid 类似，默认水平方向、可换行、水平左对齐、垂直居中、间隔 0；并授权「若 cell 能实现 flex 布局，直接复用 Grid + 加配置」。
- **反转（用户反馈）**：用户明确「flex 应直接在 cell 上加配置，不要额外加 flex 组件」——grid 级 `display` 方案被推翻。已回退：`GridNodeV2` 不引入 `display` 字段、`createFlexGridNodeV2` 工厂删除、`NodeKind` 不含 `flex`、`addFlexGrid`/`updateGridDisplay` 移除、调色板无 Flex 按钮、`GridInspector` 无「布局」切换（行数始终渲染）。
- **最终落地（cell 级）**：flex 是 `GridCellV2.flex?: boolean` 单元格属性（默认 false，存量/新建零影响）。渲染内核 `GridSchemaNode.vue` 在普通 cell 上按 `cell.flex` 分支：`cellStyle` 给 flex cell 补 `flex-wrap:wrap; justify-content:flex-start; align-items:center`；CSS 新增 `.layout-grid__cell--flex`（wrap/左/垂直居中）+ 子节点覆盖 `.layout-grid__cell--flex > .layout-p, > .layout-text { flex:0 1 auto; width:auto }`（关键：`.layout-p`/`.layout-text` 原 `width:100%` 会强制每个子节点独占一行、破坏水平流，必须覆盖）；cell 仍承载 children 递归渲染。
- `useSchemaEdits.ts` 新增 `updateSelectedCellFlex()`（勾选写 `flex:true`、取消写 `undefined`），`clearCellOverride` 一并清 `flex`；`CellInspector.vue` 顶部加「弹性布局」勾选（`data-flex`）。序列化 `normalizeNode` 透传未知字段、校验仅查 rows≥1，管线零改。
- 测试：`schema-v2-operations.test.ts` 删除工厂测试；`GridSchemaNode.flex.test.ts` 改写为 cell 级（3 例：flex 单元内联样式+子节点递归、非 flex cell 无 `--flex` 类、序列化往返保留 `cell.flex`）。
- 验证：vue-tsc 干净；全量 vitest **260/260（33 文件）** 零回归。
- 精修（同轮）：flex cell 对齐改为完全复用 cell 的 `align`/`verticalAlign` 配置——`cellStyle` 两分支合并，flex 仅多 `flex-wrap:wrap`；CSS `.layout-grid__cell--flex` 去掉硬编码 `justify-content`/`align-items`（基础 cell 已是 `display:flex` row，故只需 wrap）。默认由「左+中」变为「中+中」（继承 cell 默认），需左对齐在该 cell Inspector 设「水平对齐=左」。`GridSchemaNode.flex.test.ts` 增「`align:'left'→flex-start`」例（3→4）。验证 vue-tsc 干净、vitest **261/261（33 文件）**。
- UI 清理：移除 `CellInspector` 的「清除覆盖（恢复 Grid 默认）」按钮；`clearCellOverride` 仅被该按钮使用，顺带删除孤儿函数（useSchemaEdits 函数体 + return + DesignerApp 解构，共 3 处）。`vue-tsc` 干净、vitest **261/261（33 文件）**。
- 已知边界：flex 是单元格**自身容器行为**，与 Grid 行列结构正交——不影响 `.layout-grid__row` 渲染与边框单边规则（cell 仍是 row 内一格）；适合「一格内横排多个字段/文本」的弹性分组。
- 待办：`docs/development-plan.md` §16 仍 `[ ]` 的「组件开发文档/示例」未动；未经 git 提交（用户未授权）。

### 设计器全局快捷键（第 36 续）
- 需求：完善常见快捷键——复制/剪切/粘贴（细化落点规则）/原地复制/删除。
- 决策（AskUserQuestion 三项，全按推荐）：① 粘贴落点=智能落点；② 内存缓冲 + 全套(C/X/V/Del/Ctrl+D)；③ 输入框内全部让位原生快捷键。
- 实现：
  - `useSchemaEdits.ts`：`clipboard: FormNodeV2 | null`（深拷贝刷新 ID）+ `clipboardSourceCellId: string | null`（cut 后兜底落点）。`selectedCopiableNode()` 排除 `page`/`grid-cell`（grid 等 FormNodeV2 可复制）。`insertCloneAfterSelectedOrIntoCell(clone)` 落点优先级：选中 grid-cell→进该格；选中普通组件且其父是格→插其后(idx+1)；否则→插入槽；最终兜底→源格；均无→丢弃不提交。新增 `copySelected`/`cutSelected`/`pasteClipboard`/`duplicateSelected` 并 return。
  - `DesignerApp.vue`：`onKeydown` 重写——`isTypingTarget()`（INPUT/TEXTAREA/SELECT/contenteditable 让位原生）+ `Ctrl/Cmd+C/X/V/D` + 无修饰 `Delete`/`Backspace` 删除（`editable` 闸门）；保留原有 `Ctrl+Z/Y/S`。
- 关键修复（自检发现）：`cut` 调 `removeSelectedNode()` 会 `clearSelection()` 清空选中，导致后续 `paste` 无 `selectedNode`/`insertionSlot` 目标→静默 no-op；修复=新增 `clipboardSourceCellId`，复制/剪切时记录 `selectedOwnerCell.id` 作粘贴最终兜底。
- 类型坑（已填）：`clipboard` 起初声为 `EditorNodeV2` 导致 `appendNodeToCellV2` 报 `grid-row` 不兼容 `FormNodeV2`；收窄为 `FormNodeV2`（clipboard 仅装可复制组件）。测试 `canUndo` 断言也曾误判（setup 已提交过历史），改为「no-op 后不变」比对。
- 测试：`useSchemaEdits.test.ts` 扩 7 例（duplicate 插后/复制不改 schema/复制后粘贴在后/选中 cell 粘贴进格/cut 后粘贴回源格/非设计态全 no-op/page 与 grid-cell 不可复制）。
- 验证：vue-tsc 干净；全量 vitest **268/268（33 文件）**（261→268）。未经 git 提交（按惯例）。

### 列宽配置误变 24 修复（第 37 续）
- 现象：列宽配置值经常莫名其妙变成 24 且锁死。
- 根因两处：① `GridInspector.vue` 列宽输入 `:value="node.columns?.[columnIndex] ?? cell.width ?? 24"` 显示兜底 24（cell.width 缺省时直接显示 24，像「值变了」）；② `useSchemaEdits.parseColumnWidth` 在空/非法输入（""、`0`、负、非数字如 `.fr`/`.px`）时静默兜底返回 24，`@change` 写入 schema 且因 24 合法而永不复原——这是「经常变成 24」的主因。
- 修复：`parseColumnWidth` 改为 空串→`"1fr"`（默认）、非法→`"INVALID"` 由调用方忽略（不提交）；`updateGridColumnWidth` / `updateTableColumn`(width 分支) 遇 `"INVALID"` 直接 return；`GridInspector.vue` 显示兜底 `?? 24`→`?? "1fr"`。Table 列宽无 UI 入口但复用同解析，一并修。
- 附带发现：`DesignerApp.test.ts:315` 失败——根因是用户此前未提交改动从 `TableInspector.vue` 删除了 `.v2-hint` 说明段落（用户确认是手动移除不必要说明）。将该过时断言（要求 `.v2-hint` 存在）更新为仅校验「不含单独字段名控件」；非本次引入、非 bug。
- 测试：`useSchemaEdits.test.ts` 扩 3 例（空→1fr 非 24 / 非法不提交 / 合法 1fr·数字正常提交）。
- 验证：vue-tsc 干净；全量 vitest **271/271（33 文件）**（268→271）。未经 git 提交（按惯例）。

### 复查其他 inspector 静默兜底隐患（第 38 续 · 复查，待用户拍板改）
- 指令："复查其他 inspector 是否还有类似'静默兜底'的隐患"。结论：**隐患集中在 `useSchemaEdits.ts`（inspector 的编辑动作后端），不在 inspector 模板层**。
- inspector 模板层 `?? 数字/''`（如 `TextStyleFields.vue` 的 `fontSize??''`/`lineHeight??''`/`fontWeight??'normal'`/`color??'#111827'`，`GridInspector` 的 `rows[0]?.cells.length??1`）均为**显示层兜底、不写 schema**，安全；`schema-v2-*` 内纯默认值非输入路径，安全。
- 真正"空/非法被静默写 schema"仍存 6+1 处（已读 `useSchemaEdits.ts` 全文件核对行号）：
  1. `updateGridDimensions`(L395)：`Math.max(1, Math.floor(Number(input.value)||1))` 非法→静默 1。
  2. `updateGridCellDefault` cellPadding 分支(L490)：`raw===""?undefined:Math.max(0,Number(raw)||0)` 非法→静默 0 提交。
  3. `updateGridGap`(L501)：同上非法→静默 0 提交。
  4. `updateSelectedCellPadding`(L507)：`Math.max(0,Number(value)||0)` 空/非法→静默 0。
  5. `updateSelectedFontSize`(L573)：`Math.max(1,Math.floor(Number(raw)||1))` 非法→静默 1 提交。
  6. `updateBaseRowHeight`(L668)：`Math.max(1,Math.floor(Number(value)||8))` 非法→静默 8 提交。
  - 附：`updatePaperMargin`(L684)：`Number.isFinite(raw)?Math.max(0,Math.floor(raw)):0` 非法 NaN→静默 0 提交（同类但页级边距）。
  - 注意：曾被当作"正确范式"的 `updateSelectedCellRowHeight`(L553) 实际 `Math.max(0,Math.floor(Number(raw)))` 缺 `||0` 防护，非法→`NaN` 写入（非 0 但仍脏），应一并对齐。
- 项目**真·金标准**是 `updateSelectedLineHeight`(L576-581)：`Number.isFinite(value)&&value>0?value:undefined`（非法/空→`undefined` 移除覆盖，不写垃圾）。建议统一改法：解析用 `Number.isFinite`+范围守卫，空串或非法→`undefined`（node 更新路径）或 `return` 不提交（page/grid 级 commit 路径）。
- 现状：仅定位+报告，**未改代码、未补测试**。改动面比列宽 24 修复更大（触 page 级 baseRowHeight、grid 维度），需用户拍板是否一并修；若改则补回归测试并跑 vue-tsc+vitest（基线 271）。

### 列宽设置失效 bug（第 39 续）
- 现象（用户）：选中 grid → 列数改 2 → 第1列宽填 30mm → 不生效；点 paper 再点回 grid → 第1列变回 1fr。
- 根因（复现测试坐实，非猜测）：
  1. **主因**：`useSchemaEdits.parseColumnWidth` 不认 `mm` 后缀。标签写"列宽（mm / fr / auto）"，用户照填 `30mm` → `parseColumnWidth("30mm")` 判 `INVALID` → `updateGridColumnWidth` 直接 return 不提交。schema 仍旧，重选时 `node.columns?.[0] ?? cell.width ?? '1fr'` 落到 1fr（"没生效+变回1fr"）。裸 `30` 其实正常。
  2. **潜在 bug**：`resizeGridV2`（`schema-v2-operations.ts`）只重建 `rows`，**不同步 `grid.columns`**。渲染层 `GridSchemaNode.vue` L123-127 优先用 `columns` 作真源。默认 grid `columns` 本为 `undefined`（靠 `deriveGridColumnsV2` 重推才没炸）；但一旦设过宽（`columns` 变真实数组）或样例自带 `columns` 后，再 resize 会使 `columns` 长度错位/缺失 → 后续设列宽渲染不到。
- 修复：
  1. `parseColumnWidth` 增加 `^(\d+(?:\.\d+)?)\s*mm$` 分支，剥离 mm 后按毫米数处理（非法 mm 仍 INVALID）。
  2. `resizeGridV2` 重建行后补 `const columns = (rows[0]?.cells ?? []).map(cell => cell.width ?? "1fr"); return { ...node, rows, columns };`，长度对齐首行格数、保留既有宽、新列默认 1fr。
- 测试：临时复现测试 `repro-colwidth.test.ts` 验证后删除；正式回归加 3 例——`useSchemaEdits.test.ts` 列宽块补 `30mm`/`20 MM`→数字提交 + 改2列后设第2列 `columns[1]===40`；`schema-v2-operations.test.ts` 补 `resizeGridV2` 缩2/扩4列 `columns` 同步。
- 验证：vue-tsc 干净；全量 vitest **274/274（33 文件）**（271→274）。未经 git 提交（按惯例）。

### Table 表头样式配置（第 40 续）
- 需求：Table 表头加样式配置——字体大小、粗细、对齐。
- 数据模型：`TableNodeV2` 新增可选字段 `headerStyle?: TextStyleV2`（`schema-v2.ts`）。
- 渲染（`GridSchemaNode.vue`）：新增 `tableHeaderCellStyle(column, headerStyle)`——合并 `headerStyle` 与列 `align`（`headerStyle.align` 优先于 `column.align`），字号按 `${fontSize}px`（与 `pStyle` 同约定）、粗细 `fontWeight`；`headerStyle` 为 `undefined` 时完全回退到 `tableCellStyle(column)` 既有行为，**不破坏现存量快照**。表头 `<th>` 由 `tableCellStyle(column)` 改为 `tableHeaderCellStyle(column, node.headerStyle)`。背景：`.layout-table__cell` 默认 `justify-content:center`、`.layout-table__header` 默认 `font-weight:600`，故旧表头实际按 `column.align`（默认左）。
- 编辑层（`useSchemaEdits.ts`）：新增 `updateTableHeaderStyle(patch)`（写 `node.headerStyle`）+ 三个便捷 handler `updateTableHeaderFontSize`/`FontWeight`/`Align`；字号沿用项目金标准 `Number.isFinite(value)&&value>0?value:undefined`（非法/空→`undefined` 移除覆盖，不静默兜底成 1）。统一在 return 对象导出。
- 配置面板（`TableInspector.vue`）：在"列配置"前加"表头样式"段（字号 px / 粗细 / 水平对齐），复用全局类 `v2-sidebar__subheading`/`v2-style-grid`/`v2-control--inline`/`v2-grid-dimensions`（与 `TextStyleFields.vue` 一致）。
- 序列化 `schema-v2-serialization.ts` 用 `...node` 展开，天然保留 `headerStyle`；校验层只查 columns/rowTemplate，加可选字段安全。
- 测试：`useSchemaEdits.test.ts` 扩 4 例（设字号/非法不兜底/设粗细+对齐/非 table 不生效）；新增 `renderer-v2/__tests__/TableHeaderStyle.test.ts` 3 例（未设回退默认/应用字号px+加粗+居中/headerStyle.align 优先于 column.align）。
- 验证：vue-tsc 干净；全量 vitest **281/281（34 文件）**（274→281）。未经 git 提交（按惯例）。
- 第41续：表头字号默认16 + 结构树"折叠全部/展开全部"按钮。
  - 表头字号默认16：`TableInspector.vue` 字号输入框 `:value="node.headerStyle?.fontSize ?? 16"`（占位默认16，未设不写 schema）；渲染层 `.layout-table__header` 加 `font-size:16px`（CSS 默认，未设时无内联 fontSize、保留回退语义，不影响 TableHeaderStyle 测试"未设无内联"）；新增 `TableInspector.test.ts` 1 例锁"未设占位16、不写 schema"。
  - 结构树折叠/展开全部：新增 `composables/treeControl.ts`（`TreeControl` 接口 + `TreeControlKey` InjectionKey = `{token:Ref<number>, target:Ref<boolean>}`）；`DesignerApp.vue` 经 `provide(TreeControlKey, ...)` 下发 + `collapseAll`/`expandAll`（置 target 并 token++）；`NodeTreeItem.vue` 经 `inject` 以 `target` 作初始 `expanded`、`watch(token)` 同步重设；单独 `onToggle` 仍生效（全局信号不锁死局部切换，符合"折叠全部后仍可单独展开"）。
  - `DesignerApp.vue` `<aside>` 内 `.v2-tree` 后加"折叠全部/展开全部"两按钮 + `.v2-tree__buttons`/`.v2-tree__btn` 样式。
  - 测试：`DesignerApp.test.ts` 增 describe 2 例（初始展开→折叠全部收起→展开全部恢复；折叠后单独展开仍生效）；新建 `TableInspector.test.ts` 1 例（未设字号占位16）。
  - 验证：vue-tsc 干净；全量 vitest **284/284（35 文件）**（281→284）。未经 git 提交（按惯例）。
- 第42续：paper 页眉/页脚（MVP，进阶项按用户要求延后）。
  - 类型（`schema-v2.ts`）：新增 `HeaderFooterContentV2`（left/center/right）+ `HeaderFooterV2`（enabled/content/height/style/separator/separatorColor/separatorWidth）+ 常量 `DEFAULT_BAND_HEIGHT_MM=10`；`PaperConfigV2` 加可选 `header?`/`footer?`（**全局**，作用于所有物理页）。
  - 渲染（`GridFormRenderer.vue`）：`paperStyle` 加 `position:relative`；模板在每个物理页内、节点循环**之外**加页眉/页脚两条绝对定位带（左右受纸张边距约束、驻留上/下边距区）——**每物理页各渲染一份 = 天然每页重复**（含打印）。新增 `bandEnabled`（仅 true 才渲染）/ `bandHeight`（非法回退 10）/ `resolveBandText`（`{page}`=pp.index、`{total}`=displayedPages.length）/ `bandStyle`（字号px/加粗/颜色/分隔线）。
  - 序列化（`schema-v2-serialization.ts`）：`normalizeFormSchemaV2` 的 paper 此前**只落 size**，补 `header`/`footer` 原样保留（否则导出丢失）；废弃键 `orientation` 仍丢弃。
  - 编辑层（`useSchemaEdits.ts`）：`makeHeaderFooterUpdaters(kind)` 工厂生成 header/footer 两套动作（patch/content/enabled/height/separator/style/fontSize/fontWeight/color），数值沿用 `undefined` 金标准、空串移除该栏而非写空字符串。
  - 面板：新建 `inspectors/HeaderFooterFields.vue`（header/footer 共用一份模板，按 kind 选 handler），`PageInspector` 挂两段；`InspectorPanel` 与 `DesignerApp` 透传 `header`/`footer` props。
  - 测试：新增 `renderer-v2/__tests__/PaperHeaderFooter.test.ts`(8)、`types/__tests__/PaperHeaderFooterSerialization.test.ts`(3)、`useSchemaEdits.test.ts` +5；2 处整票/前五行快照因新增 `position:relative` 与两条 `v-if` 占位注释重生成（无其他 DOM 变化）。
  - 验证：vue-tsc 干净；全量 vitest **300/300（37 文件）**（284→300）。未经 git 提交（按惯例）。
  - 遗留（进阶，用户明确延后）：字段绑定 `{field:key}`、logo 图片、首页不同/奇偶页不同（后两者需 per-page 覆盖机制）。
- 第43续：页眉/页脚布局与高度纠错（用户用 `src/dev/ticket-schema-v2-1788784162300.json` 实测反馈）。
  - 现象：① 页眉中间标题「云南铝业股份有限公司 电气第二种工作票」（20px 加粗）被省略号截断；② 页眉区域与内容区域重叠。
  - 根因1（截断）：三区用 `flex:1 1 0` **三等分** + `text-overflow:ellipsis` → 中间只拿到 1/3 宽（≈63mm），而该标题约 95mm → 被截。**违反「左中右是对齐锚点、不是三等分固定区块」**。
  - 根因2（重叠）：该 schema `header.height=12mm` 而 `margin.top=10mm`；带子从纸张顶边往下 12mm，越过边距 2mm 伸进正文。
  - 修复（`GridFormRenderer.vue`）：
    - 布局改 `display:grid; grid-template-columns: minmax(0,1fr) auto minmax(0,1fr)` —— 中列 `auto` 取内容宽、**完整显示不省略**并自然挤压两侧；两侧 `minmax(0,1fr)` 平分剩余、放不下才省略号；带子加 `overflow:hidden`（**不换行**，超出即裁切，与 Word 一致）。
    - 高度收敛：`bandStyle` 用 `Math.min(bandHeight(band), margin.top|bottom)`，**页眉/页脚绝不超出页边距**（否则压正文 + 打印裁切）。**只影响渲染、不改写 schema**（区别于「不静默兜底」的输入解析金标准）。
    - 面板：新增 `inspectors/HeaderFooterFields.vue` 的 `margin` prop，配置高度 > 边距时显示橙色 `.v2-hint` 提示（收敛不静默）。
  - 测试：`PaperHeaderFooter.test.ts` 原「带高 12mm」用例改 8mm（合法值），新增「超过边距 → 收敛到边距、且 schema 原值 12 不动」1 例。
  - 验证：vue-tsc 干净；全量 vitest **301/301（37 文件）**（300→301）。未经 git 提交。
  - 模型要点（复用）：上边距 = 页眉高度 + 页眉到正文间距；页眉/页脚绘制在**页边距留白内**、不占正文区；可打印宽 = 纸宽 − 左右边距（页眉/页脚总宽即为此值）；三区共享同一高度，任一处过高会导致整条带裁切。
- 第44续：分页「内容溢出却不换页」修复（严重）。
  - 现象（用户）：`ticket-schema-v2-1788784162300.json` 中，grid 内容超出纸张仍不切第二页；27 行离页脚分隔线还有距离，28 行与分隔线重合，29 行覆盖页脚显示到纸张外。
  - 诊断：`tmp-diag.test.ts` 跑分页引擎确认**引擎本身正确**（30 行 → 27+3 两页，bodyHeightMm=277）。根因在渲染层真实高度校正 `measureRowHeights` 用 `getBoundingClientRect().height`，该值**含祖先 transform**——`PaperViewport` 的 panzoom `transform: scale()` 在 60% 缩放时把 10mm 行量成 6mm → 引擎误判"放得下" → 不换页。
  - 修复：
    - 新建 `renderer-v2/measure-rows.ts`：`PX_PER_MM=96/25.4` + `measureHeightMm(el)` 用 `offsetHeight`（不受祖先 transform 影响）。
    - `GridFormRenderer.vue`：`measureRowHeights` 改用 `measureHeightMm(el)`；`correctPagination` 的 `measureRow` 回调加 `Math.max(measured, gridRowHeightMm(props.schema.baseRowHeight, row))` 下限兜底（测量失真也不漏分页）；删除本地 `PX_PER_MM`，从 `measure-rows` 与 `engine-v2/pagination` 导入。
  - 测试：新建 `renderer-v2/__tests__/measure-rows.test.ts`(2)；`GridFormRenderer.pagination.test.ts` +1 例（mock `offsetHeight=1mm` 失真，断言仍切页 >1 张且确认走了测量路径）；删除临时 `tmp-diag.test.ts`。
  - 验证：vue-tsc 干净；全量 vitest **304/304（38 文件）**（301→304）。未经 git 提交（按惯例）。
  - 根因分类：分页失效**不在引擎**而在"测量被视口缩放污染"，属渲染层与视口耦合问题，已用 offsetHeight + 估算下限双修。也提示：所有依赖真实像素高度的测量都应走 `offsetHeight` 而非 `getBoundingClientRect`。
- 第45续：第38续遗留的 6+1 处 inspector 静默兜底（空/非法输入被静默写进 schema）统一改项目金标准（按用户拍板"一并修"）。
  - 范围（8 处）：`useSchemaEdits.ts` 的 `updateGridDimensions`(rows/cols 正整数)/`updateGridCellDefault`(cellPadding)/`updateGridGap`/`updateSelectedCellPadding`/`updateSelectedFontSize`/`updateBaseRowHeight`(page 级)/`updatePaperMargin`(page 级)/`updateSelectedCellRowHeight`。
  - 改法：page/grid 级 commit 路径 → 空/非法 `return` 不提交（保留现状，不静默成 1/0/8）；节点 override 路径 → 空/非法 `undefined` 移除覆盖（不静默写 0，且不写 NaN）。统一 `Number.isFinite(value)&&范围守卫?值:undefined`；抽模块级 `parseNonNegativeMm(raw)`（空/NaN→undefined，合法→Math.max(0,n)）供 padding/gap 复用。
  - 验证前基线 304/304（38 文件）；新增 `useSchemaEdits.test.ts`「第38续回归」describe 8 例（每函数：合法写值 + 空/非法不写垃圾）。最终 vue-tsc 干净；vitest **312/312（38 文件）**（304→312）。未经 git 提交（按惯例）。
  - 附：同类潜在点 `updateTableRows`（`Number(value)` 直传 `updateTableMinRowsV2`，未纳入本次 8 处）建议后续专项；`updateGridGapV2` 自带 `gap>0?gap:undefined` 守卫，旧 handler 传 0 已被 op 转 undefined（不写垃圾），本次仅统一写法。
